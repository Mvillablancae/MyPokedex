<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mypokedex" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="android:colorPrimary">#B30000</item>


        <!-- Status bar color. -->
        <item name="android:statusBarColor">#B30000</item>
        <!-- Customize your theme here. -->

        <!-- The following items are specific to minSdkVersion 24 -->
        <!-- For minSdkVersion 24, use android:windowLightStatusBar -->
        <item name="android:windowLightStatusBar">true</item>
        <!-- For minSdkVersion 24, use android:windowDrawsSystemBarBackgrounds -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>